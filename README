# Docker Helper
Copyright (C) 2022, Christophe Calmejane

## What is it?

A collection of `Dockerfile` and `compose.yml` files designed to quickly setup `Docker containers` or be used as base for new `Docker images`.
